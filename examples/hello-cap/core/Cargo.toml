[package]
name = "hello_cap_core"
version = "0.1.0"
edition = "2021"
license = "MIT OR Apache-2.0"

[lib]
crate-type = ["cdylib"]

[dependencies]
cap-core-sdk = { path = "../../../crates/cap-core-sdk" }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
